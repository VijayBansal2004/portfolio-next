export type Testimonial = {
  text: string;
  image: string;
  name: string;
  role: string;
};
export type BlogCardProps = {
  title: string;
  discription: string;
  datePosted: string;
};

export type WorkedCompanies = {
  companyName: string;
  companyLogo: string;
  companyUrl: string;
  role: string;
  duration: string;
  description: string;
};
